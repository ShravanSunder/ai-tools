# 2026-07-23-writing-tests-alias-lessons

## Scope
- Query string `writing-tests` in session mining aliases to `implementation-writing-tests` authoring work (Jul 9)
- Not evidence of a live bare `writing-tests` skill invocation

## How it worked
- Audit old PR → rewrite durable spec under current `skills-creation` → Fable advisor with absolute links → revise to great

## What failed
- First draft ceremony: universal mandatory proof fields vs conditional schema
- Weak compact-proof-line / status vocabulary / disposition composition
- Advisor packet lacked absolute skill-contract paths until user demanded them
- Stale conflicting PR treated as shipping truth would have been wrong

## Failure scenarios to pressure-test
1. Spec makes every proof field mandatory
2. Fake-backend harness scenarios claimed as skill-behavior proof
3. Advisor packet uses relative paths instead of absolute live links
4. Patch old conflicting PR instead of current-contract rewrite
5. Claim bare `writing-tests` as callable when surface is elsewhere/retired

## Takeaways / improvements
- Conditional proof schema + false-proof distinction
- Absolute paths in advisor packets by default
- Alias hygiene in session search / skill naming

## Classification
- Status: ready for `skill-audit`
- Likely owner: `implementation-writing-tests` (if shipping) + `skills-creation` contract checks
- Candidate outcome: update / continue shipping path; do not revive bare `writing-tests` name without audit
