# Persistent relationship cohesion

Named runs: (1) manage-agents, (2) canonical agent-collaboration. User directed implementation after checkpoint commit0433806. No merges, installations, CLI builds or service restarts. Preserve tables and unrelated wording.

Success: Sidekick/Advisor is a separate persistent conversation with a stable runtime/session identity reused for every follow-up, including ACPX. Worker may be native or separate per task. Reviewer retains separate independence and phase freshness rules. Main agent has no naming requirement. Management owns model/role/access/cache policy; collaboration owns Router operations only. No cold/idle/wait result causes an automatic new session. No conflation of ACPX record IDs and Router SessionRef. Runtime APIs must prove capabilities or report gaps.

Edits: manage runtime selection puts role/lifecycle before own-lineage native preference. Persistent Sidekick/Advisor uses one named ACPX session or discovered/authorized separate Router conversation. Native subagents remain fitting Worker/Operator/Reviewer route subject to author-lineage rules. Provider codex/claude/cursor references qualify native preference by required relationship. ACPX uses named session for Sidekick/Advisor, resolves existing before ensure/new, same provider-native identity for continuation; fresh session only deliberate new relationship or explicitly reported irrecoverable state. Existing no-timeout/permission/identity controls remain. Session ledger records transport and exact IDs; no new schema/daemon.

Canonical collaboration: clarify manage-agents owns worker selection/lifecycle; session-messaging reads caller-supplied relationship and reuses exact address; no replacement on missing recipient; separate creation only via supported endpoint/adapter that satisfies model/access requirements. conversation prompt --new is not a general coding launcher: current help says permission requests canceled and no model/effort fields. ACPX and Router IDs not interchangeable. Worker can participate in boards with exact identity and authority regardless of launch transport. Native parent management exclusion in frontmatter must not exclude its Router board activity. Timed wake/cache repeated26/29 policy moves to management call only for managed continuity; ordinary task schedules follow explicit user timing and current CLI capability, no cache-number copy.

Latest canonical board ref includes scoped inbox/search absent from installed CLI. Sync source from current main plus this bounded canonical edit; preserve capability-check-before-command rule. Do not install newer binary in this task.

Proof: source validators, pinned sync/check, reference/old-role search, scoped unit/type tests and focused pressure case; no runtime success inferred from docs. Current old management scenarios have stale unrelated matrix expectations; report distinctly rather than weaken them. User will pressure test broader design. Review this proposal with four distinct lenses mental-model/trigger/rule/depth; no broad redesign, source edits or subagents by reviewer.

Coordination: ai-tools current0433806 plus untracked WIP; Router isolated docs/persistent-agent-collaboration at9ecdab0. Version/changelog updates owned by ai-tools packages after coherent source lands. Broader orchestrator/design/review coordination remains remaining-skills-plan.md, not runtime scope. Permissions: reference text only, existing safe shell guidance and read-only reviewer; no new executable/security surface.

Proposal reduction: reviewer supplied complete mental-model and partial trigger/rule/depth receipts, with unread ancillary scope identified. Parent had read those management/provider references and closed that source coverage explicitly. Accepted: continuity in dispatch chain, same-relationship new-assignment reuse, persistent requirement overriding native availability, explicit unsupported Router creation capability, concrete identity comparison for ACPX continuation. Rejected: generic exclusion of all managed sessions from collaboration trigger (Router operations still belong there); clarified management routing instead. Existing route/runtime slots already hold transport, so labeled their contents without adding another schema. Removed old29 rule from canonical collaboration and routed managed maintenance to management. One bounded proposal remediation completed by parent; no re-review. Security allowed: existing docs/commands only, no widened permissions or actual session creation semantics claimed. Source implementation follows this boundary; runtime proof pending.
