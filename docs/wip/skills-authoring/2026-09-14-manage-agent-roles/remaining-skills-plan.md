# Remaining skills — discussion plan after management update

Scope: proposals for later work, not authorized implementation in the current named-skill run. Management establishes roles, harness preferences, cross-lineage Reviewer, user-controlled Advisor, subordinate naming and 26-minute Codex idle maintenance first. Its dependent caller names change now; the semantics below do not.

## Current edit status

Management and canonical collaboration cohesion edits are implemented; canonical source commit `1102ebd` is pinned in ai-tools. Devfiles companion prompt edits are in `feat/persistent-agent-instructions`. These changes are for owner inspection, not an activation or merge. Further pressure testing is paused at owner request; latest focused evidence is three semantic passes and one wake-cadence failure. Broader workflow changes below remain proposals.

## Next sequence

1. **Devfiles shared/my_agents.md (current companion edit):** concise coordinator/executor policy, delegated source collection with decisive parent verification, fitting automated/manual proof, host-aware permissions, and per-repository publication authority. Keep details in management/provider references. Proof: new substantial task delegates without user repetition; execution worker does not recursively delegate; supported/unsupported permission surfaces handled honestly.
2. **implement-plan:** separate serial delegation from parallelism. Coordinator assigns; worker implements/fixes/proves, including integration. Remove conflicting inline-parent default. Proof: dependent slices stay with worker, parent never takes back a tiny fix, changed evidence reruns only fitting proof.
3. **orchestrator-implementation-goal:** carry worker identity across phases/corrections; main owns decisions, not duplicate execution. Single observer for waits; no parent turn loop. Proof: same-worker continuation and terminal result, no repeated setup/validation.
4. **orchestrator-design, research-swarm, spec-design, program-design:** main frames and designs with user; worker gathers before full reconstruction and drafts settled meaning. Preserve semantic authority. Implement as one named-skill run per target. Proof: collection and draft work does not invent requirements/architecture; main verifies decisive sources.
5. **implementation-review and spec-program-review:** decide whether a cheaper review coordinator owns full-source reduction for substantial reviews. Preserve cross-lineage Reviewer selection and required independent coverage. Returned accepted/rejected findings retain source/requirement/reasons; main checks material findings and questionable rejections. Do not silently collapse review stages or remove reads until this contract is agreed.
6. **implementation-pr-wrapup:** one Operator owns long observation and exact-head terminal evidence; main decides. Reconcile 180-second gh-watch guidance and 120-second examples. Keep final quiet/readback gates once, not duplicated.
7. **Canonical agent-collaboration (implemented in current cohesion edit):** replace copied timing policy with management-owned scheduling decision; retain its command/identity/recovery ownership. Incorporate newer scoped inbox/search only against actual supported version; canonical Router edit -> commit -> pinned sync. No runtime timers/install/restart implied.
8. **skills-creation:** distinguish source-review reuse, produced proof claims, and ship check; preserve bounded review requirements; align new Reviewer model selection with actual author lineage. Current role-name cutover is not permission to redesign this workflow.

## Choices to discuss

- Cheaper coordinator B for substantial reviews versus main coordinator A for small work.
- Conditional final three-artifact review subsuming intermediate reviews only where no intermediate consumer requires them reviewed first.
- Main/worker drafting boundary and any direct small-task execution exception.
- Evidence for 26-minute maintenance on each actual route; cache expiry is not session expiry.
- Whether manage-agents Workflow and references/job-planning.md provide necessary dispatch sequencing, dependency/parallel-safety decisions and completion checks, or duplicate the new selection flow. Discuss before removing or redesigning either; no decision made. Preserve required reference loads, pre-dispatch history/access/model checks, receipt binding, and follow-up deltas at the owning stage; discussion pending and no implementation authorization.
- **job-planning discussion pending; no implementation authorization:** Reuse existing-plan dependencies where applicable; a standalone job graph file is not currently mandatory. Keep coordination for independent lanes, sequential bulk collection/context conservation, actual read/write safety, and examples. Decide inline versus reference; do not assume every task is one serial Sidekick, and a missing stop condition is not proof that a task must split in two.
- **job packet discussion pending; no implementation authorization:** Preserve bounded outcome, sources, authority, write scope, stop, return, and verification. Filled fields do not classify Exact steps; Complete/Partial also require a valid brief. A short Operator packet may fold facts but must retain safety, grants, target, and acceptance; omitting routine written reduction is not actual verification.
- **session ledger discussion pending; no implementation authorization:** A compact resumable handle must retain provider, cwd, named relationship/launcher when needed, configuration, assignment/evidence basis, queue observation freshness, and last qualifying model request. Clarify task-local versus runtime-home ownership; boards record meaningful checkpoints, not mutable runtime ledger state; head SHA appears only where Git correctness requires it. Preserve failure/recovery teaching.
- **token-budget discussion pending; no implementation authorization:** Prioritize parent bulk reads, turn count, receipt length, and duplicated skill text; treat these as qualitative priorities, not a measured universal ranking. Exclude rank-7 cache-prefix, hook-placement, and AGENTS.md-in-session changes by owner choice; shared 26-minute idle maintenance remains the current policy.
- **feedback disposition discussion pending; no implementation authorization:** D1 Terra/Sol default and D2 separate sessions are settled. D3 Balanced-main policy remains open. Presentation dedup is optional; do not add an automatic directional-review blacklist or same-Claude-as-cross-lineage rule.

## Preserve

Independent review, user authority, substantive proof, exact source/session identity, correct lifecycle and uncertain-result handling, controlled code scope, and honest limits. No new generic ledger, model-polling service, or copied provider recipes. The current user-selected role/matrix decisions supersede older proposed defaults in the broad audit/spec; this plan does not change runtime skills.
