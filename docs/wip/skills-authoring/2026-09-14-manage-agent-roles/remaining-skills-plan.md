# Remaining skills — discussion plan after management update

Scope: current approved driver-flow and assignment-reuse edits in this PR, followed by explicitly deferred discussion items.

## Current edit status

Management and canonical collaboration cohesion edits are implemented; the approved Workflow and inline dispatch-coordination cleanup is drafted and uncommitted, and phase-assignment reuse is authorized in this pass. Existing packet contracts are reused at their owning phases; separate session-record/ledger and model-table simplifications remain proposals. Devfiles companion prompt edits are in `feat/persistent-agent-instructions`. These changes are for owner inspection, not an activation or merge. The canonical and vendored Router six-file mirror is uncommitted, its pin remains `ed598a425f2427e32da5b0acc5f4c4b458c06799`, and official upstream commit -> pin -> sync remains pending. Further pressure testing is paused at owner request; latest focused evidence is three semantic passes and one wake-cadence failure. Broader workflow changes below remain proposals.

Ownership for the drafted flow: phases own assignments, required input, completion, proof, and results; `manage-agents` reuses those contracts and fills execution, access, and session gaps. A short direct brief is allowed only where no phase contract exists. `agent-collaboration` owns transport and message delivery; `track-show-me-your-work` owns meaningful checkpoints. Router has no upward management, tracker, role, or 26-minute policy dependency. Existing collaboration/tracker contracts are compatible.

A/B/D wording fixes are drafted in this pass: pre-dispatch acceptance and an optional direct example; a single-home flow with phase pointers; and the retained Advisor address as the consultation destination. The existing board thread is shared context; new registration or metadata remains future ownerproject work. Reviewer Grok preference remains an optional owner decision and current different-lineage coverage is unchanged. Scenario gaps, stale Delegate expectations, and pressure proof remain explicitly deferred.

## Next sequence

1. **Devfiles shared/my_agents.md (current companion edit):** the orchestrator coordinates; the executor is chosen independently for task, context, and total cost, with Balanced inline when fitting. Delegated source collection uses claimant-specific source verification, fitting automated/manual proof, host-aware permissions, and per-repository publication authority. Keep details in management/provider references. Proof: new substantial task delegates without user repetition; execution worker does not recursively delegate; supported/unsupported permission surfaces handled honestly.
2. **implement-plan (driver flow drafted this PR):** reuse the owning phase's assignment, required input, completion, proof, and result contract; the independently chosen executor implements, corrects, and proves its admitted plan; delivery remains with the orchestrator. Balanced inline execution is optional. Preserve useful Worker assignment and integration boundaries; further proof and cleanup remain proposals.
3. **orchestrator-implementation-goal (driver flow drafted this PR):** the orchestrator may be Frontier or Balanced; carry the independently chosen executor and assignment reuse across phases and corrections. Only a transfer of orchestration to another session is user-selected; executor assignment remains within task authority. Single observers handle waits; no parent turn loop. Further review and lifecycle cleanup remain proposals.
4. **orchestrator-design continuation (drafted this PR); research-swarm, spec-design, and program-design:** reuse phase assignments and their required inputs/results in the drafted driver flow; preserve semantic authority while retaining further changes in the latter skills as proposals. Collection and drafting must not invent requirements/architecture; the owning phase verifies decisive sources.
5. **implementation-review and spec-program-review:** decide whether a cheaper review coordinator owns full-source reduction for substantial reviews. Preserve cross-lineage Reviewer selection and required independent coverage. Returned accepted/rejected findings retain source/requirement/reasons; main checks material findings and questionable rejections. Do not silently collapse review stages or remove reads until this contract is agreed.
6. **implementation-pr-wrapup:** one Operator owns long observation and exact-head terminal evidence; main decides. Reconcile 180-second gh-watch guidance and 120-second examples. Keep final quiet/readback gates once, not duplicated.
7. **Canonical agent-collaboration (implemented in current cohesion edit):** use caller-supplied scheduling requirements; retain command, identity, and recovery ownership. Router has no upward management, tracker, role, or 26-minute policy dependency. Incorporate newer scoped inbox/search only against actual supported version; canonical Router edit -> commit -> pinned sync remains pending. No runtime timers/install/restart implied.
8. **skills-creation:** distinguish source-review reuse, produced proof claims, and ship check; preserve bounded review requirements; align new Reviewer model selection with actual author lineage. Current role-name cutover is not permission to redesign this workflow.

## Choices to discuss

- Cheaper coordinator B for substantial reviews versus main coordinator A for small work.
- Conditional final three-artifact review subsuming intermediate reviews only where no intermediate consumer requires them reviewed first.
- Evidence for 26-minute maintenance on each actual route; cache expiry is not session expiry.
- **Workflow and dispatch coordination:** Approved cleanup is drafted: reuse canonical-plan dependencies read-only and coordinate only separately assigned jobs with actual dependency or interference risk. Packet contracts are reused at their owning stage; preserve pre-dispatch history/access/model checks, receipt binding, and follow-up deltas.
- **job packet reuse authorized/current in this pass:** Preserve bounded outcome, sources, authority, write scope, stop, return, and verification, with authority stated once. Filled fields do not classify Exact steps; Complete/Partial also require a valid brief. A short Operator packet may fold facts but must retain safety, grants, target, and acceptance; omitting routine written reduction is not actual verification. No second generic packet or new phase-packet reuse schema is authorized here.
- **session ledger discussion pending; no implementation authorization:** A compact resumable handle must retain provider, cwd, named relationship/launcher when needed, configuration, assignment/evidence basis, queue observation freshness, and last qualifying model request. Clarify task-local versus runtime-home ownership; boards record meaningful checkpoints, not mutable runtime ledger state; head SHA appears only where Git correctness requires it. Preserve failure/recovery teaching.
- **token-budget discussion pending; no implementation authorization:** Prioritize parent bulk reads, turn count, receipt length, and duplicated skill text; treat these as qualitative priorities, not a measured universal ranking. Exclude rank-7 cache-prefix, hook-placement, and AGENTS.md-in-session changes by owner choice; the shared 26-minute idle-maintenance policy remains across models and harnesses.
- **feedback disposition discussion pending; no implementation authorization:** D1 Terra/Sol default and D2 separate sessions are settled. The orchestrator may be Frontier or Balanced; executor choice is independent by task, context, and total cost, and Balanced inline execution is optional. Only a transfer of orchestration to another session is user-selected; executor assignment remains within task authority. Presentation dedup is optional; do not add an automatic directional-review blacklist or same-Claude-as-cross-lineage rule.
- The current draft accepts existing phase assignments and results when evidence binds them; separate session-record/ledger redesign, model-table consolidation, review coordination, and remaining phase work remain proposals with no implementation authorization.

## Preserve

Independent review, user authority, substantive proof, exact source/session identity, correct lifecycle and uncertain-result handling, controlled code scope, and honest limits. No new generic ledger, model-polling service, or copied provider recipes. The current user-selected role/matrix decisions supersede older proposed defaults in the broad audit/spec; this plan does not change runtime skills.
