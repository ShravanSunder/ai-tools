# Main remains the default conversation

Revision 3. Main-authored follow-up specification and run plan. Revision 2 incorporated the owner's additional instruction to remove Terra medium from Worker choices during the initial proposal review. Revision 3 is the single bounded proposal-wording remediation; the confirmed behavior is unchanged.

## Need, basis and success

The owner explicitly chose Main as the default conversation, with direct Sidekick contact optional at the user's choice. The selected answer's recommendation applied to keeping Main as default, not to moving the conversation to the Sidekick. This is a new user-directed routing change after the earlier efficient-delegation delivery. It supersedes that delivery's default-conversation decision only. It is not another attempt to close the earlier proposal's review findings.

Success: after Main authors a ready plan and commissions an implementation Sidekick, the user continues talking to Main by default. The user may explicitly choose direct contact with the assigned Sidekick inside its scope. The Sidekick still implements, proves and corrects directly; Main does not relay every internal status turn or poll merely to keep the conversation active. Main retains all governing Requirements, Specification, Program Design, diagrams and plan authorship, material decisions, integration, assessment, acceptance and final reporting.

Classification: behavior-changing across the named runs' main paths, an existing depth reference and affected proof fixtures; triggers, runtime schemas and role authority remain unchanged. Authoring basis is user-directed intent. The navigation-session observation motivated clarification but is not proof that one model lacks general implementation ability or that these words guarantee compliance. Live model behavior remains explicitly unverified; no paid evaluation is authorized.

## Decisions

| Decision | Rationale |
| --- | --- |
| Main remains the default user conversation throughout delivery | The owner wants a capable consistent partner for interpreting requests; execution delegation does not require conversation delegation. |
| Direct Sidekick interaction is optional at the user's choice | Preserve an efficient shortcut for bounded execution without forcing it or transferring authority. |
| Sidekick remains the direct implementation/proof owner | Main's conversation role must not create a relay-only supervisor or move implementation back to Main. |
| Material questions and concise completion evidence return to Main; routine internal progress need not | Preserve judgment and acceptance without periodic expensive status relays. |
| manage-agents owns the conversation/execution distinction | Caller skills and shared prompts retain synchronized short echoes that cite manage-agents as the authoritative home; this pass does not introduce runtime transclusion or claim their prose updates automatically. |
| Remove Terra medium from execution/research Worker choices and the corresponding preference sentence | The owner explicitly requested this catalog restriction; remaining role/model choices and provider capabilities are not being redesigned. |
| Existing board seats, other model catalogs, review boundaries and cache guidance stay unchanged | This is a routing-language correction plus one Worker-choice removal, not a topology/runtime mechanism redesign. |

## Named runs

All workflow paths are beneath plugins/shravan-dev-workflow. Trigger descriptions remain unchanged.

Every active contact-default home must replace the fused “normal execution contact” wording with the two distinct responsibilities: Main is the default user conversation; the Sidekick owns direct implementation and proof. Do not merely relabel Main as the execution contact. Existing caller references to manage-agents remain the policy-owner citations; short mirrored guidance stays synchronized in this hard cutover.

The optional branch starts when the user explicitly chooses direct contact with the assigned Sidekick; a ready plan alone does not choose it. In either branch, implementation/proof remain direct and governing authorship, material questions and acceptance remain with Main. Returning to Main for conversation does not by itself pause authorized implementation or transfer execution ownership. No preference field or new protocol is introduced.

Superseded by the accepted 2026-09-23 model-routing-retier proposal: Sol low eligibility and continuity are retired. The Terra change explicitly includes the Terra-versus-Sol comparison clause under “Choose a model,” not merely the Worker table row. Its Terra-medium/Sol-low condition has no remaining eligible role after the Worker row is removed: the Sidekick and Review catalogs already contain no Terra entry. Revise that sentence surgically: remove the Terra comparison while retaining Sol-low eligibility for matching assignments and continuity of suitable existing Sol-low relationships. Preserve other role choices, provider capability inventories and evaluator configuration. Existing negative fixtures forbidding Terra for unrelated roles remain valid; do not broaden this pass to clean them up.

1. **manage-agents**: change the Main flow and Sidekick authority prose to teach Main-default conversation, user-chosen direct Sidekick contact, and unchanged direct execution/proof. Remove the Terra-medium Worker row and revise the selection sentence to remove only the Terra comparison, retaining Sol-low eligibility and existing-relationship continuity; use the remaining existing role-table choices without inventing an unrelated default. Update the affected execution-contact pressure scenario to cover default and explicit user-choice branches, and any affected fixture text asserting Terra is Worker-eligible. Preserve the concrete-benefit delegation test, role/seat mapping, other catalogs and waiting policy. Provider capability inventories may still list an available model; the evaluator's Terra judge configuration is not a Worker assignment, stays unchanged and is not invoked in this pass. Main path/proof change; no new reference or schema.
2. **orchestrator-design**: align design-ready continuation with commissioning execution rather than automatically handing off the user's conversation. Preserve Main-authored planning and all design/visual obligations. Update its affected scenario and registered criteria/inventory, including misleading titles or identifiers if needed as one hard cutover. Main path/proof change; no new branch beyond consuming the owner's optional-contact rule.
3. **orchestrator-implementation-goal**: align body and goal-contract-and-routing reference with the same distinction. Preserve material-question return, direct Sidekick execution, meaningful checkpoints, Main assessment, independent review and PR terminal. Update affected scenario expectations/registry where they teach the old default. Main path/depth/proof change; no new coordination field or protocol.

Companion surfaces: reconcile the same wording in ai-tools AGENTS.md and workflow README, and Devfiles shared/my_agents.md. Preserve the completed Act-mode authorized-assumption filter, scope gate, Soul and all unrelated prompt sections. Keep existing harness/provider references unchanged unless a directly contradictory contact-default sentence is found; do not duplicate policy into all providers.

## Coordination and delivery

- ai-tools current worktree, branch `research/lane-ownership-and-model-cost`, starting HEAD `0119c8beee270db6964a70fb50ad1c83c525691b`, clean. PR #83 is the existing publication target. Fetched `origin/main` at `7f92364b9fbd617cb5748dfbd65fb255f471a27e` has no additional commits behind the branch.
- Companion work reuses its retained implementer/worktree and existing PR; private identity and commit details belong only in its private commission.
- Main obtains independent proposal review for this new commission, then retained implementers execute the three named runs sequentially in ai-tools and the independent companion wording change. Main assesses current source/proof before independent implementation review and refreshes affected PR descriptions against final heads.
- Workflow metadata advances to `2.19.0` in applicable manifests/marketplaces. Agent-router remains `0.9.0`; canonical vendor/pin/source stay unchanged. Add a concise dated routing-follow-up changelog and proof reference as needed; retain previous entries as history rather than silently rewriting their completed outcomes.
- The older efficient-delegation spec remains historical; Main marks its conversation-default decision superseded by this follow-up without erasing its review record.

## Proof and boundaries

Use current-source cross-reference/stale-policy searches, affected scenario parser/registry/unit checks, typecheck, official changed-skill validators, manifest/JSON/version/whitespace checks and protected-companion-section comparisons. Scenarios express actual user situations; do not place expected answer vocabulary into prompts to manufacture results. No live pressure subject, external semantic judge, Frontier experiment, production Router exercise, or fake behavioral pass. Source/static proof cannot establish actual model compliance.

Before implementation, review this proposal through skills-creation; after implementation and fitting proof, Main assesses the diff and the independent lead reviews current changed files. Limit coverage to this delta and affected consumers, not the completed earlier feature or unrelated failure backlog. Mechanical PR publication follows accepted review. No merge is authorized.

Non-goals: Soul consistency change, implement-plan authority-filter work, stop-hook/harness fixes, wake/cache automation, model/effort changes beyond the explicit Terra-medium Worker removal, switching existing agent sessions, native/top-level role changes, new board seat or Observer, Router source/vendor updates, home/cache apply, dependency refresh without a demonstrated need, merge/rebase/force-push/tag.

Review status: revision 3 accepted to implement. The retained independent lead reduced four complete initial proposal lanes, verified the owner-authorized Terra amendment, and closed the single bounded wording remediation without lane redispatch. Main accepts the source-backed verdict `great`; current behavior proof remains owner-deferred. Private reviewer identities and transcripts remain in the work trail, not this document.
