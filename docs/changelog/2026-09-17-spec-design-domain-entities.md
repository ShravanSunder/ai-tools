# 2026-09-17 spec-design domain entities

- Marketplace plugin: `shravan-dev-workflow` `2.15.0`
- Affected skill: `spec-design` (R1 of the accepted multi-run spec `docs/wip/skills-authoring/2026-09-17-domain-model-and-trace/proposal.md`)
- Affected files: `SKILL.md` (description, opening chain, boundary, stage 2, stage 5 completion, Required Why/What Views, completion blockers); new `references/domain-entities.md`; `references/requirements-and-traceability.md`; `references/artifact-and-self-review.md`; `shared-references/requirements-specification-program-design.md`
- Behavior: the Specification now defines the domain entities its obligations name — stable `E` identifiers, identity rule, relationships with cardinality, invariants, observable states — before writing obligations over them; obligations over undefined nouns are a completion blocker; an entity map is a Required Why/What View; definitions stay implementation-free and binding to packages/schemas/wire shapes is routed to `program-design`; the identity chain is `U -> E -> P -> O -> R -> C -> V`.
- Trigger: the description gains the domain-entity loading condition and the entity-binding boundary to `program-design`.
- Proof: new pressure scenario `spec-design-define-entities-before-obligations` (fixture + `cases.ts`), reproduction class `representative comparison`. RED against the unmodified skill: subject returned `decision-needed` asking the owner to define entities, all four semantic criteria 0.00. GREEN after the change: all evaluators 1.00, subject produced `E1..E3` with identity rules deciding the re-hold case, cardinalities, invariants, named states, and obligations citing `E` ids. Full `spec-design` suite 11/11 (one variance failure on an untouched criterion of `use-helpful-diagrams` passed on rerun). Trigger evaluation on descriptions only, five prompts: data-model definition → `spec-design`, undecided term → `discuss-pathfinding`, package/wire shape → `program-design`, controls correct. Existing `spec-design-use-helpful-diagrams` criterion rewritten to include the entity map. Behavior proof is one scenario family on one subject model; real-repo behavior remains unverified until the next design session.
- Validation at authoring: `pnpm --dir tests/skills exec tsc --noEmit` passed; scenario-loader unit tests passed (one pre-existing unrelated failure in `minimal-planning-delivery-contract.test.ts`); `claude plugin validate .` passed; Codex marketplace manifest parsed.
- Local Codex/Claude/Cursor caches were not refreshed; refresh is a post-merge step.
