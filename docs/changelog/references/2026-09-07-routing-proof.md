# Routing and work-trail proof

The agreed orchestrator boundary is source-backed next-skill or stop selection. These Luna cases do not prove downstream design, planning, implementation, review, or publication. Tracker rehearsal cases test the requested explanation/classification; actual Markdown conversion examples are separate.

## Validation

- `pnpm --dir tests/skills run test:unit`: 121 passed across 17 files, exit 0.
- `pnpm --dir tests/skills run typecheck`: exit 0.
- All three skill quick-validations, Claude plugin and marketplace validation, and `git diff --check`: passed.
- Full Luna routing/explanation run: 25 passed, 3 failed, exit 1. All route decisions were correct; the secondary failures required unasked logging-name or identity recitals.
- Two existing recovery-admission classifications: 1 passed, 1 failed, exit 1. The failure demanded hypothetical branches absent from its prompt.
- Independently reviewed scope corrections were retested: 5 passed, exit 0, including one previously passing case whose criterion was also simplified.
- Current coverage across the full runs and affected-case retests: all 28 routing/explanation cases and both recovery-admission cases pass. Case identities and tool-call caps are unchanged. This is combined evidence, not a single uninterrupted 30-case green run.
- Subjects: `gpt-5.6-luna`, high reasoning. Judge: `gpt-5.6-sol`, medium reasoning. Child processes use the verified read-only adapter and isolated lifecycle hooks.

Run the main matrix with `pnpm --dir tests/skills exec vitest run evals --config vitest.config.ts --maxConcurrency 4 -t 'track-show-me-your-work|orchestrator-design|orchestrator-implementation-goal'`, setting the subject and judge environment variables documented in `tests/skills/README.md` to the models above. The two additional cases are `review-implementation-admit-bounded-recovery-review` and `spec-program-review-admit-one-recorded-recovery-review`.

Independent proposal, skill-source, harness, full-package, and scenario-scope reviews found no remaining source blocker. The parent verified the bounded findings and kept phase readiness distinct from routing readiness. Earlier execution-oriented scores remain historical evidence of an incorrectly expanded test contract, not failures of the agreed routing contract.

The unchanged instruction-only tracker also has actual Luna clean and partial Markdown-conversion examples covering corrected claims, malformed records, missing detail, and cutoff handling. No Python storage helpers remain. Raw transcripts and process notes stay outside the published package. Installed-plugin refresh and merge are separate actions.

## September 8 policy amendment

The owner approved one preferred spec/design review-and-correction round, a substantive second round, and explicit approval before a third, plus shorter recovery/terminal wording. All five affected decision/classification scenarios pass on Luna/high with Sol/medium judging (exit 0); 121 unit tests, typecheck, skill validators, plugin validation, and independent source review also pass. This is targeted validation of the final policy change, not a rerun of the earlier full matrix.
