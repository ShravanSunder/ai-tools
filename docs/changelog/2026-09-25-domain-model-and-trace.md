# 2026-09-25 Domain model and trace in design

- `shravan-dev-workflow` 2.63.0. Implements runs 1 and 2 of `docs/wip/skills-authoring/2026-09-25-domain-model-and-trace/proposal.md` (Revision 2).
- `program-design`: new description. New step 5 "Bind the domain model" comes after alternatives and before composition (old steps 5-17 are now 6-18). Every Specification entity gets a semantic owner, a package or module home, a schema/type home, the shape at each boundary it crosses, and `persisted | derived | cached`. Contracts follow the repository's type conventions, and result and decision contracts are closed variants. `components-ownership-interfaces.md` teaches convention discovery, the existing-code rules, and a filled binding-and-shape template.
- `program-design`: one trace table, `U · R · E · owner · interface · shape and home · state · failure · proof`, lives in the artifact. `proof-architecture-and-traceability.md` builds it, and the returned coverage disposition is derived from its rows. Required Views gain an entity -> home map; the data/event flow view shows the shape on each boundary-crossing edge.
- `program-design`: the owner gate (step 16) shows the binding table, trace table, one entry-to-effect path, and "deviations and unresolved decisions", then ends the turn with `decision-needed`. Review (step 17) runs on a later turn, or in the same turn after an explicit owner waiver, which is recorded as `structural-realization confirmation: waived by owner`.
- `spec-program-review`: program-only review checks bindings, boundary shapes, and closed variants. Three-artifact review walks the trace table row by row and resolves each design noun against the entity table: an entity-like noun with no `E` gets `Route: spec-design`, and a synonym gets `Route: program-design`. A recorded owner waiver counts as the confirmation.
- Scenarios: ports `bind-entities-before-components`, `carry-durable-trace-table`, `stop-for-owner-before-review`, `check-trace-table-rows`, and `catch-noun-leak`; adds `flag-missing-binding`; rewrites the `choose-helpful-diagrams` criterion. `docs/diagram-vocabulary.md` gains the new view token.
- Validation: skill tests 123/123, typecheck, `claude plugin validate .`, and `rg -n 'orchestrator-(design|implementation-goal)' plugins/shravan-dev-workflow/skills/{program-design,spec-program-review}` returns no matches. Trigger evaluation: [11/11 routed as expected](references/2026-09-25-domain-model-trace-trigger-evaluation.md). Live behavior evals were not run because Codex credits were unavailable, so behavior is drafted from intent and not yet evaluated.
- Codex, Claude, and Cursor cache refresh/reinstall: pending post-merge.
