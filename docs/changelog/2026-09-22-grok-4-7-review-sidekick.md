# Grok 4.7 sidekick and reviewer rows

- Marketplace plugin: `shravan-dev-workflow` `2.25.0`.
- Affected skill: `manage-agents`.
- Grok 4.7 `low` or `medium` is Balanced for implementation and research Sidekicks and for reviewers.
- Reviewers use Sol `medium`, a Frontier row, or Grok 4.7 `low`, `medium`, or `high`. Grok 4.7 `high` is Frontier and reviewer-only.
- Workers do not select Grok.
- Validation: role-table inspection. Live catalog probe and cache refresh not run.
