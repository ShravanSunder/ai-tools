# Router MCP schema-driven collaboration guidance

- Bumped `agent-router` to `0.10.0` for Router MCP and CLI collaboration guidance.
- Vendored canonical source from the accepted Router commit `4b85ce55e35ae38dd5776e239f8e954f952152a2`.
- Runtime skill teaches live-schema MCP use; the canonical Router human guide owns manifest-based registration instructions.
- Kept approval separate from confinement; do not read provider transcripts or session files.
- Preserved vendor-only agent metadata and the canonical source pin.
- Validation: canonical/vendor quick validators, equality, JSON/version/pin, link/length, public-privacy, diff, and Claude marketplace checks passed; Codex discovery reports `agent-router` 0.10.0 available from local source. No cache or home refresh was performed; runtime behavior remains unverified.
