{
  "type": "object",
  "additionalProperties": false,
  "required": ["cot", "decision", "reason"],
  "properties": {
    "cot": {
      "type": "string",
      "description": "Exactly 2 sentences: current job and mode, what [last] did, and whether a user answer still gates the next work"
    },
    "decision": {
      "type": "string",
      "enum": ["continue_work", "stop_ok"]
    },
    "reason": {
      "type": "string",
      "description": "1-2 sentences. continue_work design: resume design/discussion, do not implement. continue_work implementation: resume implement/fix/prove. stop_ok: one short justification, 1-2 sentences. Continue reasons must not choose among a pending user decision."
    }
  }
}
