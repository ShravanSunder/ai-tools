# Source Inspirations Moved

The broad source-inspiration catalog is no longer maintained in
`plugins/shravan-dev-workflow/references/`.

Use `plugins/shravan-dev-workflow/docs/source-inspiration-catalog.md` instead.

This compatibility pointer exists so older docs and installed-cache references do
not hard-break. Runtime workflow skills should load phase-specific references,
not this source catalog.
