interface:
  display_name: "Plan Creation Swarm"
  short_description: "Create implementation plans with lanes"
  default_prompt: "Use plan-creation-swarm to create a written implementation plan from a semantically current pair-ready specification/program-design review result, or from work proven implementation-mechanics-only, without editing code. For one named runtime skill package, require explicit skills-creation parent authority."
