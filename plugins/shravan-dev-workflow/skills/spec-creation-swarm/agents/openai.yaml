interface:
  display_name: "Spec Creation Swarm (Legacy)"
  short_description: "Run legacy fixed-swarm spec creation"
  default_prompt: "Use spec-creation-swarm to create this spec with bounded subagents before writing an implementation plan."
